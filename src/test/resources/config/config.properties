# =========================
# Dummy configuration
# Change these values for your application
# =========================

browser=chrome
headless=false

# Demo UI is served by the framework itself
baseUrl=http://localhost:8085

# Demo API
apiBaseUrl=http://localhost:8085/api

# H2 demo DB. Replace with Oracle URL/user/password in real project.
db.url=jdbc:h2:mem:axisdb;DB_CLOSE_DELAY=-1
db.username=sa
db.password=
db.driver=org.h2.Driver

api.username=demoUser
api.password=demoPassword
api.token=dummy-token

explicitWait=15
pageLoadTimeout=30

screenshot.dir=target/screenshots
report.dir=target/extent-report
